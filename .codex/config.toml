# Project defaults optimized for normal implementation sessions. Select the
# model per task/thread; do not pin a model slug here because workspace model
# availability may differ across Codex surfaces.
model_reasoning_effort = "medium"
model_reasoning_summary = "concise"
model_verbosity = "low"
tool_output_token_limit = 8000

[tools]
web_search = { context_size = "low" }

[mcp_servers.supabase]
url = "https://mcp.supabase.com/mcp?project_ref=icqzbvtjisxwycvioiup"
